{
  "name": "deep-dive-studio",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Topic -> researched, fact-checked, longform documentary podcast script (and audio).",
  "scripts": {
    "generate": "tsx src/index.ts",
    "ui": "tsx src/server.ts",
    "typecheck": "tsc --noEmit",
    "build": "tsc"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.111.0",
    "dotenv": "^16.4.5"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "tsx": "^4.19.0",
    "typescript": "^5.7.0"
  }
}
